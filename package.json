{
  "name": "paytonnguyen-creator.github.io",
  "private": true,
  "description": "Build tooling for the site. Nothing here ships to the browser — the pages are plain HTML and the Degree Ledger is bundled into a single file ahead of time.",
  "scripts": {
    "build:ledger": "node tools/build-ledger.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.25.10",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  }
}
